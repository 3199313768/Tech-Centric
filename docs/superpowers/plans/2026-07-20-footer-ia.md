# Footer IA Slim + Desktop More Menu Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 精简页脚为「主 Tab + 联系」，并把技能工坊/资源的桌面发现路径补到顶栏「更多」下拉。

**Architecture:** `SiteFooter` 只渲染 `SITE_NAV_TABS` 与邮箱/GitHub。抽出可复用的 `SiteNavMoreMenu` 客户端组件，挂入 `Navigation` 与 `KnowledgeNav` 的桌面链接区；紧凑抽屉已含 `SITE_NAV_SECONDARY`，保持不变。e2e 同步改断言。

**Tech Stack:** Next.js 16、React 19、SpiritGarden CSS（`sg-nav-*`）、Playwright。

**Spec:** `docs/superpowers/specs/2026-07-20-footer-ia-design.md`

---

## File Structure

| 文件 | 职责 |
|------|------|
| Create `src/components/home/shell/SiteNavMoreMenu.tsx` | 桌面「更多」下拉：开关、Escape、外点关闭、次级链接 |
| Modify `src/components/home/shell/SiteFooter.tsx` | 移除次级与展柜等链接 |
| Modify `src/components/home/shell/Navigation.tsx` | 桌面链接区挂入 More 菜单 |
| Modify `src/components/knowledge/shell/KnowledgeNav.tsx` | 同上，保持双壳对齐 |
| Modify `src/app/globals.css` | `.sg-nav-more*` 样式（token，无 hex） |
| Modify `e2e/core-paths.spec.ts` | 页脚/更多断言 |

---

### Task 1: 更新 e2e（先红）

**Files:**
- Modify: `e2e/core-paths.spec.ts`

- [ ] **Step 1: 替换页脚次级入口测试**

将：

```ts
  test('页脚含技能工坊与资源次级入口', async ({ page }) => {
    await page.goto('/')
    const footer = page.locator('.sg-footer-links')
    await expect(footer.getByRole('link', { name: '技能工坊' })).toBeVisible()
    await expect(footer.getByRole('link', { name: '资源' })).toBeVisible()
  })
```

改为：

```ts
  test('页脚仅主 Tab，无次级与庭园附加入口', async ({ page }) => {
    await page.goto('/')
    const footer = page.locator('.sg-footer-links')
    await expect(footer.getByRole('link', { name: '庭院' })).toBeVisible()
    await expect(footer.getByRole('link', { name: '归档' })).toBeVisible()
    await expect(footer.getByRole('link', { name: '草本集' })).toBeVisible()
    await expect(footer.getByRole('link', { name: '档案馆' })).toBeVisible()
    await expect(footer.getByRole('link', { name: '园主' })).toBeVisible()
    await expect(footer.getByRole('link', { name: '技能工坊' })).toHaveCount(0)
    await expect(footer.getByRole('link', { name: '资源' })).toHaveCount(0)
    await expect(footer.getByRole('link', { name: '展柜' })).toHaveCount(0)
    await expect(footer.getByRole('link', { name: '庭园志' })).toHaveCount(0)
    await expect(footer.getByRole('link', { name: '庭园度量' })).toHaveCount(0)
    await expect(footer.getByRole('link', { name: '工作台' })).toHaveCount(0)
  })

  test('桌面更多菜单可进入技能工坊', async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 800 })
    await page.goto('/')
    const more = page.getByRole('button', { name: '更多' })
    await expect(more).toBeVisible()
    await more.click()
    await page.getByRole('menuitem', { name: '技能工坊' }).click()
    await expect(page).toHaveURL(/\/skills/)
  })
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm test:e2e e2e/core-paths.spec.ts --grep "页脚仅主 Tab|桌面更多"
```

Expected: 页脚测试因仍有次级链接失败；「更多」因按钮不存在失败。

- [ ] **Step 3: Commit**

```bash
git add e2e/core-paths.spec.ts
git commit -m "$(cat <<'EOF'
test: 页脚精简与桌面更多菜单断言

EOF
)"
```

---

### Task 2: 精简 SiteFooter

**Files:**
- Modify: `src/components/home/shell/SiteFooter.tsx`

- [ ] **Step 1: 改为只渲染主 Tab**

完整替换为：

```tsx
import Image from 'next/image'
import Link from 'next/link'
import { personalInfo } from '@/data/site/personal'
import { SITE_NAV_TABS } from '@/lib/site/routes'

export function SiteFooter() {
  const email = personalInfo.socialLinks.email?.replace(/^mailto:/, '') ?? ''

  return (
    <footer className="sg-footer sg-footer--garden">
      <div className="sg-footer-top">
        <p className="sg-footer-copy-left">
          © {new Date().getFullYear()} SpiritGarden
        </p>
        <nav className="sg-footer-links" aria-label="页脚导航">
          {SITE_NAV_TABS.map((tab) => (
            <Link key={tab.href} href={tab.href}>
              {tab.label}
            </Link>
          ))}
        </nav>
        <div className="sg-footer-social">
          {email ? (
            <a href={`mailto:${email}`} className="sg-footer-email" aria-label={`发送邮件至 ${email}`}>
              {email}
            </a>
          ) : null}
          {personalInfo.socialLinks.github ? (
            <a
              href={personalInfo.socialLinks.github}
              target="_blank"
              rel="noopener noreferrer"
              aria-label="GitHub"
            >
              <Image src="/spirit-garden/icon-share.png" alt="" width={18} height={20} aria-hidden unoptimized />
            </a>
          ) : null}
        </div>
      </div>
    </footer>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/home/shell/SiteFooter.tsx
git commit -m "$(cat <<'EOF'
refactor(footer): 页脚仅保留主 Tab 与联系入口

EOF
)"
```

---

### Task 3: 新增 SiteNavMoreMenu + 样式

**Files:**
- Create: `src/components/home/shell/SiteNavMoreMenu.tsx`
- Modify: `src/app/globals.css`（在 `.sg-nav-link` 相关样式附近追加）

- [ ] **Step 1: 创建组件**

```tsx
'use client'

import { useEffect, useId, useRef, useState } from 'react'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { ChevronDown } from 'lucide-react'
import { isSiteNavActive, SITE_NAV_SECONDARY } from '@/lib/site/routes'

export function SiteNavMoreMenu() {
  const pathname = usePathname()
  const [open, setOpen] = useState(false)
  const rootRef = useRef<HTMLDivElement>(null)
  const menuId = useId()

  useEffect(() => {
    if (!open) return

    const onPointerDown = (event: MouseEvent) => {
      if (!rootRef.current?.contains(event.target as Node)) {
        setOpen(false)
      }
    }
    const onKeyDown = (event: KeyboardEvent) => {
      if (event.key === 'Escape') setOpen(false)
    }

    document.addEventListener('mousedown', onPointerDown)
    window.addEventListener('keydown', onKeyDown)
    return () => {
      document.removeEventListener('mousedown', onPointerDown)
      window.removeEventListener('keydown', onKeyDown)
    }
  }, [open])

  useEffect(() => {
    setOpen(false)
  }, [pathname])

  return (
    <div className="sg-nav-more" ref={rootRef}>
      <button
        type="button"
        className={`sg-nav-link sg-nav-more__trigger${open ? ' sg-nav-more__trigger--open' : ''}`}
        aria-expanded={open}
        aria-haspopup="menu"
        aria-controls={menuId}
        onClick={() => setOpen((value) => !value)}
      >
        更多
        <ChevronDown size={14} aria-hidden className="sg-nav-more__chevron" />
      </button>
      {open ? (
        <div id={menuId} className="sg-nav-more__menu" role="menu" aria-label="次级导航">
          {SITE_NAV_SECONDARY.map((tab) => (
            <Link
              key={tab.href}
              href={tab.href}
              role="menuitem"
              className={`sg-nav-more__item${isSiteNavActive(pathname, tab.href) ? ' sg-nav-more__item--active' : ''}`}
              onClick={() => setOpen(false)}
            >
              {tab.label}
            </Link>
          ))}
        </div>
      ) : null}
    </div>
  )
}
```

- [ ] **Step 2: 追加 CSS**

在 `src/app/globals.css` 的 `.sg-nav-link` / `.sg-nav-links` 样式块之后追加：

```css
.sg-nav-more {
  position: relative;
}

.sg-nav-more__trigger {
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

.sg-nav-more__chevron {
  transition: transform 0.2s ease;
}

.sg-nav-more__trigger--open .sg-nav-more__chevron {
  transform: rotate(180deg);
}

.sg-nav-more__menu {
  position: absolute;
  top: calc(100% + 8px);
  left: 0;
  min-width: 148px;
  padding: 8px;
  border-radius: 12px;
  border: 1px solid rgba(238, 224, 210, 0.55);
  background: var(--sg-parchment);
  box-shadow: 0 8px 24px rgba(26, 36, 24, 0.12);
  display: flex;
  flex-direction: column;
  gap: 2px;
  z-index: 220;
}

.dark .sg-nav-more__menu {
  background: rgba(16, 24, 16, 0.96);
  border-color: rgba(161, 212, 148, 0.18);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
}

.sg-nav-more__item {
  display: block;
  padding: 8px 12px;
  border-radius: 8px;
  text-decoration: none;
  color: var(--sg-ink);
  font-size: 14px;
  line-height: 1.4;
  transition: background-color 0.15s ease, color 0.15s ease;
}

.sg-nav-more__item:hover,
.sg-nav-more__item:focus-visible {
  background: rgba(188, 240, 174, 0.35);
  color: var(--sg-green-deep);
  outline: none;
}

.sg-nav-more__item--active {
  color: var(--sg-green-deep);
  font-weight: 600;
}

.dark .sg-nav-more__item {
  color: rgba(232, 245, 228, 0.86);
}

.dark .sg-nav-more__item:hover,
.dark .sg-nav-more__item:focus-visible,
.dark .sg-nav-more__item--active {
  color: var(--sg-green-light);
  background: rgba(161, 212, 148, 0.14);
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/home/shell/SiteNavMoreMenu.tsx src/app/globals.css
git commit -m "$(cat <<'EOF'
feat(nav): 桌面次级入口「更多」下拉

EOF
)"
```

---

### Task 4: 接入 Navigation 与 KnowledgeNav

**Files:**
- Modify: `src/components/home/shell/Navigation.tsx`
- Modify: `src/components/knowledge/shell/KnowledgeNav.tsx`

- [ ] **Step 1: Navigation 桌面链接区挂入**

在 `Navigation.tsx` 顶部增加导入：

```tsx
import { SiteNavMoreMenu } from '@/components/home/shell/SiteNavMoreMenu'
```

将桌面链接区改为：

```tsx
        <div className="sg-nav-links sg-nav-desktop-only">
          {SITE_NAV_TABS.map((tab) => (
            <Link
              key={tab.href}
              href={tab.href}
              className={`sg-nav-link${isSiteNavActive(pathname, tab.href) ? ' sg-nav-link--active' : ''}`}
            >
              {tab.label}
            </Link>
          ))}
          <SiteNavMoreMenu />
        </div>
```

抽屉内 `SITE_NAV_SECONDARY` 保持不动。

- [ ] **Step 2: KnowledgeNav 同样改动**

导入 `SiteNavMoreMenu`，在 `.sg-nav-links.sg-nav-desktop-only` 的主 Tab map 后追加 `<SiteNavMoreMenu />`。

- [ ] **Step 3: 跑 e2e**

```bash
pnpm test:e2e e2e/core-paths.spec.ts --grep "页脚仅主 Tab|桌面更多|顶栏 5 Tab|T4"
```

Expected: PASS（「顶栏 5 Tab」仍断言桌面链接区无「技能工坊」直链，下拉内项不在 `.sg-nav-links` 的 `getByRole('link')` 平铺计数冲突——技能工坊在菜单打开前不在 DOM，关闭时 `toHaveCount(0)` 仍成立）。

- [ ] **Step 4: lint + typecheck**

```bash
pnpm lint && pnpm typecheck
```

Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/components/home/shell/Navigation.tsx src/components/knowledge/shell/KnowledgeNav.tsx
git commit -m "$(cat <<'EOF'
feat(nav): 双壳顶栏接入更多菜单

EOF
)"
```

---

## Spec Coverage Check

| Spec 要求 | Task |
|-----------|------|
| 页脚仅主 Tab + 邮箱 + GitHub | Task 2 |
| 移除次级与展柜等 | Task 2 |
| 桌面「更多」承载 SITE_NAV_SECONDARY | Task 3–4 |
| 紧凑抽屉不变 | Task 4（明确不改抽屉） |
| e2e 更新 | Task 1 |
| lint / typecheck | Task 4 Step 4 |
| T4 邮箱 mailto | Task 4 Step 3 回归 |

## Non-goals（不做）

- 页脚视觉大改、多列布局
- 展柜/庭园志/度量/工作台进「更多」
- 改 RAG / 园主页联系文案
